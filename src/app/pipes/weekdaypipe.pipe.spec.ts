import { WeekdaypipePipe } from './weekday.pipe';

describe('WeekdaypipePipe', () => {
  it('create an instance', () => {
    const pipe = new WeekdaypipePipe();
    expect(pipe).toBeTruthy();
  });
});
